name: Deploy TanStack Start site to Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build-and-deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v1

      - name: Install Dependencies
        run: bun install

      - name: Build Project
        run: bun run build

      - name: Determine Output Path & Prepare Files
        run: |
          if [ -d "./dist" ]; then
            TARGET="./dist"
          elif [ -d "./.output/public" ]; then
            TARGET="./.output/public"
          elif [ -d "./.output" ]; then
            TARGET="./.output"
          else
            TARGET="."
          fi
          echo "BUILD_PATH=$TARGET" >> $GITHUB_ENV

          # GitHub Pages CNAME 유지
          echo "aifind.kr" > "$TARGET/CNAME"

          # SPA 404 방지
          if [ -f "$TARGET/index.html" ]; then
            cp "$TARGET/index.html" "$TARGET/404.html" || true
          fi

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload Artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ${{ env.BUILD_PATH }}

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
